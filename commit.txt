feat: import do módulo 'PrismaClient' no componente api.jsx.

feat: após o envio dos dados para o Banco de Dados, os estados do 'useState' são zerados

fix: componente 'handler' do arquivo 'api,jsx' é responsavel pela comunicação da aplicação com o MongoDB via a biblioteca 'Prisma'. Duas rotas foram reorganizadas (GET e POST)

fix: função 'handler' é assincrona e chama partes diferentes de acordo com as rotas 'GET' e 'POST'

fix: data tem um limite para ser adicionado pelo usuário




